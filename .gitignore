# Generated by XcodeGen from project.yml — never commit the derived project.
Agrent.xcodeproj/
Agrent.xcworkspace/
Agrent/Info.plist

# Xcode local state
xcuserdata/
DerivedData/
build/
*.xcuserstate
.DS_Store

# Agent worktrees. Real git checkouts, so `git add -A` from the root stages
# them as embedded repositories — which it did once, into a PR.
.claude/
